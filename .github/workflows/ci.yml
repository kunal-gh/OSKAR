name: OSKAR Enterprise CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  code-quality:
    name: Lint & Format (Black, Isort, Flake8)
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"
        cache: 'pip'
        
    - name: Install Code Quality Tools
      run: |
        python -m pip install --upgrade pip
        pip install black flake8 isort
        
    - name: Format with Black (Check Only)
      run: black --check engine/src/ engine/tests/
        
    - name: Check Imports with Isort
      run: isort --check-only engine/src/ engine/tests/
        
    - name: Lint with Flake8
      run: flake8 engine/src/ engine/tests/

  unit-tests:
    name: Automated Unit Testing (Pytest)
    runs-on: ubuntu-latest
    needs: code-quality
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"
        cache: 'pip'
        
    - name: Install Core Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r engine/requirements.txt
        pip install pytest pytest-cov
        
    - name: Execute Test Suite
      env:
        OSKAR_API_KEY: ${{ secrets.OSKAR_API_KEY }}
        OSKAR_ADMIN_KEY: ${{ secrets.OSKAR_ADMIN_KEY }}
        OSKAR_SYSTEM_KEY: ${{ secrets.OSKAR_SYSTEM_KEY }}
        PYTHONPATH: "."
      run: |
        cd engine
        pytest tests/ -v --cov=src --cov-report=term-missing
